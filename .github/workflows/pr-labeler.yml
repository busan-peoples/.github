name: Label PRs

on:
  - workflow_call
  - pull_request:
      types: [opened]
      branches-ignore:
        - "main"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: srvaroa/labeler@master
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
